import 'package:flutter/material.dart';

class InsightsPages extends StatelessWidget {
  const InsightsPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
